# Axle Detection B-WIM — Python dependencies
# Install via: pip install -r requirements.txt
#
# PyTorch: choose ONE of the two lines below, then uncomment it.
# CPU only (smaller download, fine for testing):
# torch>=2.2.0
#
# CUDA 12.1 (recommended if you have an NVIDIA GPU):
# torch>=2.2.0 --index-url https://download.pytorch.org/whl/cu121
#
# The setup_env.ps1 script will ask which one you want.

numpy>=1.26
scipy>=1.12
scikit-learn>=1.4
tqdm>=4.66
matplotlib>=3.8
pandas>=2.2
